from sklearn.datasets import load_iris
from sklearn.ensemble import RandomForestClassifier
import joblib, os

X, y = load_iris(return_X_y=True)
model = RandomForestClassifier(n_estimators=100, random_state=42)
model.fit(X, y)

os.makedirs("models", exist_ok=True)
joblib.dump(model, "models/model.pkl")
print("✅ Model trained and saved!")
```

**Step 6 — Run it**
```
python src/train.py
```
You should see: `✅ Model trained and saved!`

**Step 7 — Create .gitignore** (so we don't upload junk)

Create a file called `.gitignore` in the main folder with this inside:
```
venv/
__pycache__/
*.pkl
.env
```

**Step 8 — Push everything to GitHub**
```
git add .
git commit -m "Lab 1: ML project setup"
git push